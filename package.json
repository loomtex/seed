{
  "name": "seed-controller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx/esm --test src/test/*.test.ts",
    "acceptance": "node --import tsx/esm src/acceptance/index.ts"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.0.0",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
